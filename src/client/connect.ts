'use strict';
import ConnectOptions from './connect-options';
import {Server} from '../server';





// TODO: ...
export default function connect(options: ConnectOptions): Server {
    // TODO: sort out options...
    return new Server(options);
}
