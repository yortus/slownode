

// TODO: global augmentations go here... They are picked up internally, eg by unit test code, but how to expose these for slownode consumers?


//declare namespace Promise {
//    function delay<U>(ms: number, value?: U): Promise<U>;
//}


//declare function closure(env: { [name: string]: any; }, fn: Function): (...args) => any;


//declare var EPOCH: string;
