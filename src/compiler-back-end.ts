'use strict';
import {Node} from "babel-types";             // Elided (used only for types)
import Task from './task';





// TODO: ...
export function emit(ast: Node): Task {
    throw new Error(`Not implemented`);
}
